<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nageoffer.onecoupon.distribution.dao.mapper.CouponTaskMapper">

    <update id="markInputCompleted">
        UPDATE t_coupon_task
        SET input_completed = 1,
            update_time = NOW()
        WHERE id = #{taskId}
          AND input_completed = 0
    </update>

    <!-- 完成屏障的“批次是否全部终态”检查由 Finalizer 先在带 shop_number 分片键的查询中完成；
         这里避免在 unsharded t_coupon_task 与分片 batch 表之间做跨库子查询。 -->
    <update id="markSuccessIfAllBatchesFinished">
        UPDATE t_coupon_task
        SET status = 3,
            completion_time = NOW(),
            update_time = NOW()
        WHERE id = #{taskId}
          AND status = 1
          AND input_completed = 1
    </update>
</mapper>
